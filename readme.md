# TwillGenerator

## Installation

Via Composer

Add repo:

```json
{
  "repositories": [
    {
      "type": "vcs",
      "url": "https://github.com/haringsrob/twill-generator"
    }
  ]
}
```

Require:

``` bash
$ composer require haringsrob/twill-generator
```
